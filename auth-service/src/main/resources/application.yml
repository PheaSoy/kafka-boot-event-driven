server:
  port: 8082
spring:
  cloud.stream.bindings:
    login-out.destination: auth.login # Topic Name
    login-out.content-type: application/json
    #auth.login-in:.destination: auth.login # Topic Name